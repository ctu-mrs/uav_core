name: experiment
root: ./
startup_window: gazebo
tmux_options: -f ./tmux.conf
windows:
  - gazebo:
      layout: tiled
      synchronize: after
      panes:
        - ssh uav3
        - ssh uav4
        - ssh uav5
        - ssh uav7
        - ssh uav9
